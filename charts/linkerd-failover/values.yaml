# Docker image for the operator
image:
  registry: cr.l5d.io/linkerd
  name: failover
  tag: latest

# -- Determines which `TrafficSplit` instances to consider for failover
labelSelector: "app.kubernetes.io/managed-by=linkerd-failover"
